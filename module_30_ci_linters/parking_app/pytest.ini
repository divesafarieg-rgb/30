[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --strict-markers

markers =
    parking: Tests for parking entry and exit operations
    slow: Tests that are slow to run
    integration: Integration tests